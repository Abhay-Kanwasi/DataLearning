# Write a Pandas program to compare the elements of the two Pandas Series.

import pandas as pd
sample_data = [2, 4, 6, 8, 10], [1, 3, 5, 7, 10]
ds1 = pd.Series(sample_data[0])
ds2 = pd.Series(sample_data[1])
